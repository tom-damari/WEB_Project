// Get Update Account Details Button
const updateAccountDetails = document.querySelector('.updateUserDetailsButton');

// Add event listeners to remove icons
updateAccountDetails.addEventListener('click', (e) => {
    e.preventDefault();
    window.alert('לא מימשנו במסגרת הפרויקט');
});